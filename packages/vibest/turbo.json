{
	"$schema": "https://turborepo.com/schema.v2.json",
	"extends": ["//"],
	"tasks": {
		"dev": {
			"dependsOn": ["build"],
			"outputs": ["dist"],
			"inputs": ["./src/node/**"],
			"interruptible": true
		},
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		}
	}
}
